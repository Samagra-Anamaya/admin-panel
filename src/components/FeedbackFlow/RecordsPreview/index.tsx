import { Step, StepLabel, Stepper } from '@mui/material';
import { useState } from 'react';
import styles from './index.module.scss';

const RecordsPreview = (props: any) => {

    return (
        <div className={styles.container}>
        </div >
    );
}

export default RecordsPreview